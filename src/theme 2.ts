import { createTheme } from '@mui/material/styles';

const theme = createTheme({
 palette: {
    primary: {
      main: "#A6455B",
    },
    secondary: {
      main: "#FFFFFF",
      light: "#F3AEA4"
    }
  }
});

export default theme